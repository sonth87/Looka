node_modules
dist
release
.turbo
.next
build
out
coverage
*.log
.DS_Store
.env
.env.local
captures/
model-store/
database/*.db
*.tsbuildinfo

# Electron Build & Packaging Artifacts
release/
*.dmg
*.exe
*.zip
*.blockmap
builder-debug.yml
builder-effective-config.yaml


# Runtime assets fetched by scripts/fetch-assets.mjs (MediaPipe wasm + models,
# sql.js wasm). Regenerated by dev/build, so they are derived, not source —
# ~32 MB per app of binaries that would otherwise land in every clone.
apps/*/public/wasm/
apps/*/public/models/
