fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.8.0
numpy>=1.26.0
httpx>=0.27.0

# Added for /card-photo, /background, /retouch, /identity-similarity.
# All confirmed installable and runnable CPU-only (no GPU) in this
# environment on 2026-09-08 -- see the report for this change for what's
# a real ML path vs. a documented classical fallback.
opencv-contrib-python>=4.10.0
mediapipe>=1.0.0
facenet-pytorch>=2.6.0
